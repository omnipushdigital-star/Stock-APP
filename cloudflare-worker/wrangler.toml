name = "atsl-trading-scheduler"
main = "worker.js"
compatibility_date = "2024-01-01"

# Cron trigger — fires every minute
[triggers]
crons = ["* * * * *"]

# Secrets (set via: wrangler secret put TRADING_CRON_SECRET)
# TRADING_CRON_SECRET = your CRON_SECRET value from .env.local
